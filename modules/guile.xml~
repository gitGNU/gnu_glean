<?xml version="1.1" standalone='no' ?>
<!DOCTYPE question,solution SYSTEM "hello.dtd">
<quiz xml:lang="en-GB">

<question>"Hello, World!"</question>
<solution>"World, Hello!"</solution>


</quiz>;;; Test Problems — these should normally be stored in 
;;; the appropriate file in the modules structure.
;;; To achieve this:
;;  - decide on internal storage format
;;  - create helper script to store files thus
;;  - how are problems best loaded into the library?
;;  - Devise translation of plain text to guilecraft format.
(define (predicate-q question) 
  (string-append "What is the predicate for " question))

(define problem1 
  (problem "What is the predicate for the empty list?" "null?" 'list-predicates))
(define problem2
  (problem "What is the predicate for numbers?" "number?" 'number-predicates))
(define problem3
  (problem (predicate-q "strings?") "string?" 'string-predicates))
(define problem4
  (problem (predicate-q "boolean?") "boolean?" 'boolean-predicates))


;;; Problems below stands in for a dynamically generated
;;; list of problems, parsed from the xml module files.

(define problems 
  (list problem1 problem2 problem3 problem4))

